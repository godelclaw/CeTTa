; Prototype umbrella for CeTTa list design experiments.
;
; Current split:
; - list: fast normal list interface backed by expressions
; - clist: explicit cons-list interface for inductive semantics

!(import! &self list)
!(import! &self clist)
